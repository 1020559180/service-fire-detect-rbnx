# Lifecycle contract: rbnx boot starts the fire & smoke detection driver via Driver(CMD_INIT).
[contract]
id      = "robonix/service/fire_detect/driver"
version = "1"
kind    = "service"
idl     = "lifecycle/srv/Driver.srv"
description = "Lifecycle management for the fire_detect fire & smoke detection primitive."

[mode]
type = "rpc"
