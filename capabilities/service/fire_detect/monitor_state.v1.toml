# Read the latest result of the real-time fire & smoke monitor (alert / detection boxes / GPS / timestamp), for polling by an external loop.
[contract]
id      = "robonix/service/fire_detect/monitor_state"
version = "1"
kind    = "service"
idl     = "fire_detect/srv/MonitorState.srv"
description = "Read the latest result of the real-time fire & smoke monitor (alert/detections/gps/timestamp), for polling by an external loop."

[mode]
type = "rpc"
