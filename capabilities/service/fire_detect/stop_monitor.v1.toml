# Stop the real-time fire & smoke monitor: end the background detection thread.
[contract]
id      = "robonix/service/fire_detect/stop_monitor"
version = "1"
kind    = "service"
idl     = "fire_detect/srv/StopMonitor.srv"
description = "Stop the real-time fire & smoke monitor (end the background detection thread)."

[mode]
type = "rpc"
