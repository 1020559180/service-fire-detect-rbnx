# Detect fire & smoke (flame/smoke) in the drone camera's current frame, return detection boxes; attach the current GPS on a hit.
[contract]
id      = "robonix/service/fire_detect/fire_detect"
version = "1"
kind    = "service"
idl     = "fire_detect/srv/FireDetect.srv"
description = "Detect fire & smoke (flame/smoke) in the drone camera's current frame, return detection boxes, and attach the current GPS coordinates on a hit."

[mode]
type = "rpc"
