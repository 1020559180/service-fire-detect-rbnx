float64 min_confidence  # confidence threshold (0~1); detections below it are filtered out; <=0 means use the configured default
---
std_msgs/String status  # JSON: {"success":..., "detections":[{"class","confidence","bbox":[x,y,w,h]}], "gps":{...}|null, ...}
